[phases.build]
nixPkgs = ['nodejs-18']

[phases.setup]
nixPkgs = ['nodejs-18']

[staticAssets]
# Serve nothing

[phases.install]
nixPkgs = ['nodejs-18']
cmds = ['npm ci --only=production']

[phases.build]
nixPkgs = ['nodejs-18']
cmds = []

[phases.runtime]
nixPkgs = ['nodejs-18']
